services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - app-data:/data
    environment:
      - DB_PATH=/data/token_speed.db
      - SECRET_KEY=change-this-to-a-random-string
    restart: unless-stopped

volumes:
  app-data:
